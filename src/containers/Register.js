import React from 'react';
import { withRouter } from "react-router-dom"; 

function Register() {
    return (
        <>
            Register
        </>
    );
}

export default withRouter(Register);
