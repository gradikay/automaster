import React from 'react';
import { withRouter } from "react-router-dom"; 

function Footer() {
    return (
        <>
            NotFound
        </>
        );
}

export default withRouter(Footer);
